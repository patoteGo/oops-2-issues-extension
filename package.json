{
  "name": "oops-2-issues",
  "version": "2.0.0",
  "description": "oops 2 issues — Manifest V3 Chrome extension that captures full or region screenshots and files GitHub issues via a Personal Access Token.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "zip": "npm run build && cd dist && zip -r ../oops-2-issues.zip . -x '*.DS_Store'",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
